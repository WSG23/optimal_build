CVE-2026-26013 | platform | 2026-05-31 | langchain-core is flagged via vision token counting SSRF, but this repo refuses remote image URLs and only passes controlled data URLs in backend/app/services/ai/multi_modal_analyzer.py while the 1.x LangChain upgrade remains a larger compatibility project.
CVE-2026-4539 | platform | 2026-06-30 | Pygments is a transitive tooling dependency with no upstream fix version yet; the advisory requires local crafted input rather than a production request path.
CVE-2025-71176 | platform | 2026-06-30 | pytest is present only for the repository test toolchain; upgrading the test stack to pytest 9.x is a separate compatibility task and does not affect production request handling.
CVE-2026-41488 | platform | 2026-06-30 | langchain-openai 1.x requires a broader LangChain core upgrade; current Capture paths do not pass attacker-controlled remote image URLs into LangChain token sizing.
CVE-2026-41481 | platform | 2026-06-30 | langchain-text-splitters is a transitive LangChain dependency; the affected URL splitter is not used for user-submitted URLs in Capture and the LangChain 1.x upgrade remains a separate compatibility task.
PYSEC-2024-278 | platform | 2026-06-30 | langchain-community flags TFIDFRetriever.load_local deserialization/SSRF history; this codebase does not load attacker-controlled TFIDFRetriever artifacts, and the safe fix is part of the broader LangChain 1.x compatibility upgrade.
PYSEC-2024-277 | platform | 2026-06-30 | joblib advisory concerns loading untrusted pickle/cache artifacts; production code must not deserialize user-supplied joblib files, and the advisory has no fixed version.
PYSEC-2026-89 | platform | 2026-06-30 | Python-Markdown is transitive tooling/runtime support; application paths should not render attacker-controlled Markdown without request guards, and the reported package version is newer than the advisory fixed baseline.
