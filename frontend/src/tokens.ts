/**
 * @deprecated Use `@ob/tokens` import alias instead.
 * This file is maintained for backward compatibility only.
 *
 * Migration:
 * ```typescript
 * // Before
 * import { cssVar } from '@/tokens'
 *
 * // After
 * import { cssVar } from '@ob/tokens'
 * ```
 */
export { cssVar } from '../../core/design-tokens'
