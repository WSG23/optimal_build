# =============================================================================
# OPTIMAL BUILD - Environment Configuration
# =============================================================================
# SECURITY WARNING: Never commit real credentials to version control!
# Copy this to .env and customize for your environment.
#
# PRODUCTION CHECKLIST:
# [ ] SECRET_KEY: Generate with `python -c "import secrets; print(secrets.token_urlsafe(32))"`
# [ ] FIRST_SUPERUSER_PASSWORD: Use strong password (16+ chars)
# [ ] POSTGRES_PASSWORD: Use unique, strong password
# [ ] S3_ACCESS_KEY/S3_SECRET_KEY: Use unique credentials
# [ ] ENVIRONMENT: Set to "production"
# [ ] BACKEND_ALLOWED_ORIGINS: Only your production domains
# =============================================================================

# Database
POSTGRES_SERVER=localhost
POSTGRES_USER=postgres
POSTGRES_PASSWORD=change-me-in-production
POSTGRES_DB=building_compliance
BUILDABLE_USE_POSTGIS=true
SLOW_QUERY_THRESHOLD_SECONDS=0.5

# Security - CHANGE THESE IN PRODUCTION!
SECRET_KEY=dev-only-change-in-production-min-32-chars
API_RATE_LIMIT=10/minute
ENVIRONMENT=development

# CORS
# Comma separated list or JSON array of origins allowed to call the backend API.
BACKEND_ALLOWED_ORIGINS=["http://localhost:3000","http://localhost:5173","http://localhost:4400"]

# Services
REDIS_URL=redis://localhost:6379
S3_ENDPOINT=http://localhost:9000
S3_ACCESS_KEY=change-me-in-production
S3_SECRET_KEY=change-me-in-production

# CAD SDK Licenses
# Provide vendor-issued license keys via local secrets or deployment-time injection.
ODA_LICENSE_KEY=

# Task Processing
CELERY_BROKER_URL=redis://localhost:6379/0
CELERY_RESULT_BACKEND=redis://localhost:6379/1
RQ_REDIS_URL=redis://localhost:6379/2

# Overlay Job Queues
OVERLAY_QUEUE_LOW=overlay:low
OVERLAY_QUEUE_DEFAULT=overlay:default
OVERLAY_QUEUE_HIGH=overlay:high

# Storage Buckets
IMPORTS_BUCKET_NAME=cad-imports
EXPORTS_BUCKET_NAME=cad-exports

# Admin - CHANGE THESE IN PRODUCTION!
FIRST_SUPERUSER=admin@buildingcompliance.com
FIRST_SUPERUSER_PASSWORD=change-me-use-strong-password

# Jurisdiction-Specific (Hong Kong)
# HK_DATA_GOV_API_KEY is optional; DATA.GOV.HK endpoints are usually public
HK_DATA_GOV_API_KEY=public
# HK_GEOSPATIAL_TOKEN is required only for protected CSDI/LandsD geospatial APIs
HK_GEOSPATIAL_TOKEN=

# Jurisdiction-Specific (New Zealand)
# NZ_LINZ_API_KEY is optional for public datasets; required for rate-limited LINZ APIs
NZ_LINZ_API_KEY=public

# Jurisdiction-Specific (Seattle)
# Optional SODA app token for Seattle/King County open data portals
SEATTLE_SODA_APP_TOKEN=public

# Jurisdiction-Specific (Toronto)
# Optional SODA/CKAN app token for Toronto open data
TORONTO_SODA_APP_TOKEN=public

# Frontend Maps/Geocoding
# Provide for interactive map + Google geocoding
VITE_MAPBOX_ACCESS_TOKEN=
VITE_GOOGLE_MAPS_API_KEY=
