# Secrets allowlist for check_secrets.py
# Format: one full secret string per line. Comments start with #.
#
# Only allowlist KNOWN-SAFE values:
#  - Documentation placeholders ("account-password")
#  - Test fixtures already gated by the SKIP_PATH_PARTS test dirs
#  - Public example values from vendor docs
#
# NEVER allowlist real production credentials. Rotate them instead.

# Documentation placeholder for OneMap setup (docs/jurisdictions/sg.md)
account-password
